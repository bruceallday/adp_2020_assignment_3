import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(({
  root: {
    width: 500,
    marginLeft: 'auto',
    marginRight: 'auto',
  },

}));

export default useStyles;