import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(({
  root: {
    width: 500,
    marginLeft: 'auto',
    marginRight: 'auto',
  },

  formField: {
    margin: 10,
  },

}));

export default useStyles;
